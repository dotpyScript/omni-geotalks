import LoginPage from '@/components/auth/LoginPage';

export const metadata = {
  title: 'Admin Sign In — IEGS',
  description: 'Secure admin access for IEGS platform management.',
};

export default function LoginRoute() {
  return <LoginPage />;
}
